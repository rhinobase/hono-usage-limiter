<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Architecture

<!-- lore:019fd5b3-072e-7b9a-ba84-26e46539417f -->
* **Freestyle Cloud usage-limiter: org-member-scoped + CachedUsageStore wrapper**: Freestyle Cloud's usage tracking (apps/server) is always scoped to the active organization member (member ID), never directly to userId. \`resolveUsageOwner(db, userId, activeOrgId)\` resolves membership (active org first, else earliest membership as fallback across orgs), caching (user,org)→memberId for 1hr; every user has a guaranteed auto-created personal-workspace membership, so \`NoOrganizationError\` should be rare and is caught → HTTP 400 (never let it 500). \`CachedUsageStore\` wraps \`D1Store\` with a 2-min Cache API layer on \`getBucket()\` (raw D1 read is ~88ms) — but \`deduct()\` and \`getLedger()\` always bypass cache and hit D1 directly (D1 is atomic/authoritative: inner.deduct() runs first, then the cache entry is patched in-place, never invalidated) specifically to avoid overspending against a stale cached bucket. This is an intentional fail-open soft gate — an over-budget owner can slip through for up to the TTL within a single colo. Same owner-resolution helper (\`resolveUsageOwnerId\`) is shared between the HTTP middleware and the streaming SttSession Durable Object.

<!-- lore:019f0945-4dfe-774f-9d2a-b202f485e520 -->
* **hono RPC type chain: Client\<T>, PathToChain, ClientRequest, InferResponseType**: From hono@4.8.3 \`client/types.d.ts\`: - \`Client\<T>\`: extracts schema \`S\` from \`HonoBase\`, maps keys via \`PathToChain\<K, S>\` - \`PathToChain\`: recursively splits path on \`/\` into nested object; leaf is \`ClientRequest\<E\[Original]>\`; empty segment → \`"index"\` key - \`ClientRequest\<S>\`: per-method function \`(args, options?) => Promise\<ClientResponseOfEndpoint\<S\[M]>>\`; \`HasRequiredKeys\<R>\` makes args optional - \`ClientResponse\<T,U,F>\`: \`ok\` is \`true/false/boolean\` based on status; \`json()\` returns \`Promise\<T>\` if \`F extends "json"\`, \`Promise\<never>\` if text - \`InferResponseType\<T,U>\`: extracts output type filtered by status code - \`hc\<T extends Hono>(baseUrl, options?) => UnionToIntersection\<Client\<T>>\` RPC type accuracy depends on \`validator()\` \`out\` type flowing correctly through \`ToSchema\`.

<!-- lore:019f093a-58dd-782b-ac5a-b17b7dcd1021 -->
* **hono ValidationTargets and key helper types — exact definitions**: From hono@4.8.3 dist/types/types.d.ts: - \`ValidationTargets\<T=ParsedFormValue, P=string>\`: \`{ json: any; form: Record\<string, T|T\[]>; query: Record\<string, string|string\[]>; param: Record\<P, P extends \\\`${infer \_}?\\\` ? string|undefined : string>; header: Record\<RequestHeader|CustomHeader, string>; cookie: Record\<string, string> }\` - \`HasUndefined\<T>\` (in @hono/standard-validator): \`undefined extends T ? true : false\` - \`Input = { in?: {}; out?: {}; outputFormat?: ResponseFormat; }\` - \`FormValue\`: \`string | Blob\`; \`ParsedFormValue\`: \`string | File\` - \`HandlerResponse\<O>\`: \`Response | TypedResponse\<O> | Promise\<Response | TypedResponse\<O>>\` - \`MiddlewareHandler\<E,P,I>\`: \`(c: Context\<E,P,I>, next: Next) => Promise\<Response | void>\` - \`ToSchema\`, \`MergeSchemaPath\`, \`MergeEndpointParamsWithPath\` handle RPC schema building.

<!-- lore:019f033c-4bd6-7865-8bd8-9af861df9ac8 -->
* **hono-credit package structure: dual CJS/ESM with /memory subpath export**: Package: \`hono-usage-limiter\` v0.1.0, repo \`rhinobase/hono-usage-limiter\`. 4 subpath exports: \`.\`, \`./memory\`, \`./unstorage\`, \`./d1\`. Built with pkgroll, dual CJS/ESM. Key types: \`UsageBucket\`, \`UsageStore\`, \`UsageStoreFactory\` (\`(c: unknown) => UsageStore\`), \`UsageManager\`, \`UsageManagerOptions\`, \`UsageStatus\`, \`UsageEnv\`, \`UsageManagerConfig\` (field: \`store: UsageStore | UsageStoreFactory\`, \`defaultUsage\`, \`defaultWindowDurationMs\`, \`keyGenerator\`, \`autoProvision\`). Store resolved per-request in middleware via \`typeof storeOrFactory === 'function' ? storeOrFactory(c) : storeOrFactory\`. Release system: craft-based with 5 workflow files. CI: 'CI Tests' always passes; 'Release preview' always fails (pkg-pr-new app not installed \[\[019f079e-b492-75a2-98bb-9cf427bde55c]]).

<!-- lore:019f033c-4bd0-7db2-b646-09ffb156cbfa -->
* **hono-credit: DB-agnostic credit management via CreditStore adapter interface**: hono-usage-limiter is Hono-focused but database-agnostic — users implement \`UsageStore\` (5 methods: \`getBucket\`, \`createBucket\`, \`updateBucket\`, \`deduct\`, \`getLedger\`). \`UsageManagerConfig.store\` accepts \`UsageStore | UsageStoreFactory\` where \`UsageStoreFactory = (c: unknown) => UsageStore\`. Middleware resolves store per-request via \`typeof\` check. \`UsageManager\` (manager.ts) takes \`UsageManagerOptions\` with concrete \`store: UsageStore\` only. 3 store impls: \`MemoryStore\` (testing), \`D1Store\` (Cloudflare D1, requires 2 pre-created tables), \`UnstorageStore\` (any unstorage driver, O(n) \`updateBucket\` issue). Middleware injects \`UsageManager\` as \`c.get('usage')\` — does not auto-deduct. Known issues: \`MemoryStore.getBucket()\` returns mutable reference; \`totalConsumed\` JSDoc misleading.

<!-- lore:019f0945-4dfd-7e59-99fc-789339133526 -->
* **hono-openapi ecosystem package signatures — exact versions and exports**: Confirmed installed versions in hono-openapi: \`@standard-community/standard-json@0.3.5\`, \`@standard-community/standard-openapi@0.2.9\`, \`@standard-schema/spec@1.0.0\`, \`@hono/standard-validator@0.2.0\`, \`hono@4.8.3\`. \`@standard-schema/spec@1.0.0\` exports ONLY \`StandardSchemaV1\` interface with \`InferInput\<S>\` and \`InferOutput\<S>\` — no other inference utilities. \`@standard-community/standard-json\` exports: \`toJsonSchema\` (quansync sync/async dual), \`loadVendor\`, \`ToJsonSchemaFn\`. No inference utilities. \`@standard-community/standard-openapi\` exports: \`toOpenAPISchema\` (async, returns \`Promise<{schema, components}>\`), \`loadVendor\`, \`ToOpenAPISchemaContext\` (\`{components, options?: Record\<string,unknown>}\`), \`ToOpenAPISchemaFn\`. No inference utilities. Neither standard-community package exports type inference helpers — all inference flows through \`@standard-schema/spec\` \`StandardSchemaV1.InferInput/InferOutput\`.

<!-- lore:019fd5b6-e7c7-77a9-86e5-622904db79f2 -->
* **hono-openapi has no tsconfig.json — build/lint gated by pkgroll+biome, not tsc**: rhinobase/hono-openapi has no \`tsconfig.json\` in the repo; running \`tsc\` directly fails with TS5057 (config not found). Build is via \`pkgroll --clean-dist\` (esbuild-based, no typecheck) and lint/format via \`biome check .\` — there is no strict \`tsc\` gate in CI or locally. Running an ad-hoc manual typecheck surfaces pre-existing TS7053 'implicitly any' warnings on dynamic string/symbol indexing in handler.ts and utils.ts (indexing OperationObject/PathItemObject with dynamic keys) — these predate any given change and are consistent with the codebase's existing style; don't treat them as new regressions introduced by your PR.

<!-- lore:019caf12-49c0-73f8-86a4-0634fbe887e9 -->
* **hono-openapi requestBody.required generation in getSpec()**: In \`src/handler.ts\`, the \`getSpec()\` function builds \`requestBody\` for \`json\` and \`form\` validator targets. There are two branches: 1. \*\*Creating new requestBody\*\* (when \`docs.requestBody\` doesn't exist or has no content) — this is where \`required: true\` should be set (fixed in PR #223) 2. \*\*Appending to existing requestBody\*\* (else branch) — only adds media content, doesn't touch \`required\` since the object already exists (possibly from \`defaultOptions\`) Per OpenAPI 3.1, \`requestBody.required\` defaults to \`false\`, so it must be explicitly set to \`true\`. Using \`validator('json', schema)\` or \`validator('form', schema)\` inherently means the body is required — if a user wanted optional, they'd use \`describeRoute\` with manual \`requestBody\` config. The fix affects all vendors uniformly since the code path is shared in \`handler.ts\`, not per-vendor.

<!-- lore:019caee4-adca-7417-9656-d0cff25719de -->
* **hono-openapi resolver() option passing has two different nesting levels**: In hono-openapi's \`resolver()\` (\`src/middlewares.ts\`), \`userDefinedOptions\` flows to two functions with different nesting: 1. \*\*\`toJsonSchema(schema, options)\`\*\* from \`@standard-community/standard-json\` — flat options \`{ fallback: ... }\`. Signature: \`QuansyncFn\<JSONSchema7, \[schema: StandardSchemaV1, options?: Record\<string, unknown>]>\` (sync/async dual via quansync). 2. \*\*\`toOpenAPISchema(schema, context)\`\*\* from \`@standard-community/standard-openapi\` — \`{ components?, options?: { fallback: ... } }\` where nested \`options\` is passed to \`toJsonSchema\` internally. Returns \`Promise<{ schema: OpenAPIV3\_1.SchemaObject; components: ... }>\`. Vendor-specific defaults (ArkType fallback, Zod v4 date override) are injected only into the \`toOpenAPISchema\` path's nested \`options\`. Changing flat \`toJsonSchema\` options affects all vendors.

<!-- lore:019f0934-dcef-72b9-ab8b-5a24a1edb378 -->
* **hono-openapi test suite structure: per-vendor test files with snapshot + assertion tests**: Each supported schema vendor has its own test file in src/\_\_tests\_\_/: zodv3.test.ts, zodv4.test.ts, valibot.test.ts, arktype.test.ts, effect.test.ts, sury.test.ts, typebox.test.ts, plus basic.test.ts for cross-cutting concerns. Tests mix snapshot assertions (for full spec shape) with explicit property assertions (e.g. requestBody.required === true, schema.$ref). basic.test.ts covers path parameter leakage, operationId, hide behavior, and composed handlers. Each vendor file tests: basic usage, metadata/ref, and vendor-specific edge cases (e.g. ArkType morph schemas, Zod v4 z.date() conversion, Valibot transformations).

<!-- lore:019f0945-4dff-7867-9e19-10e3a631aa9d -->
* **hono-openapi utils.ts: removeExcludedPaths always includes parameterized routes**: In \`src/utils.ts\`, \`removeExcludedPaths()\` excludes: explicit exclude list (string/regex), wildcards without path params (\`\*\` without \`{\`), and static file heuristic (last segment has \`.\` AND no \`{\` in path). The static file exclusion is \`shouldExcludeAsStaticFile = !hasPathParameters && looksLikeStaticFile\` — parameterized routes (e.g. \`/users/{id}.json\`) are always included even if they look like static files. This is intentional behavior.

<!-- lore:019f0932-9247-72af-8e39-b81446befac4 -->
* **hono-openapi validator() I type parameter: in vs out mapping for ValidationTargets**: hono-openapi validator() has 8 generic type params: (1) Schema extends StandardSchemaV1, (2) Target extends keyof ValidationTargets, (3) E extends Env, (4) P extends string, (5) In = StandardSchemaV1.InferInput\<Schema>, (6) Out = StandardSchemaV1.InferOutput\<Schema>, (7) I extends Input = { in: \[In] extends \[ValidationTargets\[K]] ? In : InferInput\<In, K> (with HasUndefined optionality); out: {\[K in Target]: Out} }, (8) V extends I = I. \`HasUndefined\<T> = undefined extends T ? true : false\` controls optionality. \`in\` uses \`InferInput\` utility (ported from \`@hono/zod-validator\`) to preserve literal unions while falling back to wire types for wide types. \`out\` always uses schema Out. Return type is \`MiddlewareHandler\<E, P, V>\`. Package versions: @hono/standard-validator@0.2.2, @standard-schema/spec@1.0.0, hono@4.8.3.

<!-- lore:019f096b-0ba3-703a-bcec-3aa54552b67e -->
* **InferInput utility in @hono/standard-validator: literal union preservation via IsLiteralUnion**: \`packages/standard-validator/src/utils.ts\` (PR #2008) ports \`InferInput\` from \`@hono/zod-validator/src/utils.ts\` verbatim (only JSDoc differs). Key types: \`IsLiteralUnion\<T, Base>\` (uses \`UnionToIntersection\` trick to detect literal unions); \`IsOptionalUnion\<T>\` (true if \`undefined extends T\` but not \`unknown\`/\`any\`); \`InferInputInner\<Output, Target, T>\` — per-key: preserves if \`IsLiteralUnion\`, preserves if \`IsOptionalUnion\`, else falls back to \`T|T\[]\` (form), \`string|string\[]\` (query), \`string\` (param/header/cookie), \`unknown\` (json). \`InferInput\<Output, Target>\` wraps with \`{}\` guard and optional handling. Imports \`FormValue\`, \`ParsedFormValue\`, \`ValidationTargets\` from \`hono/types\`; \`UnionToIntersection\` from \`hono/utils/types\`.

### Decision

<!-- lore:019fd59b-13dd-72e4-b782-6c046a7cb743 -->
* **hono-openapi PR #238: conditional OpenAPI 3.2 version bump for QUERY method**: PR #238 unconditionally bumps emitted \`openapi\` field from "3.1.0" to "3.2.0" to support HTTP QUERY method's dedicated \`query\` Path Item field (3.2-only; absent in 3.1, so 3.1 validators reject it). Verified via official migration guide (learn.openapis.org/upgrading/v3.1-to-v3.2.html): 3.2 is fully backward-compatible with 3.1 at document-structure level, no breaking changes overall. Recommended fix: "conditional versioning" — emit 3.2.0 only when a route actually uses a 3.2-only construct (query op or additionalOperations), else keep 3.1.0. Rejected the blanket bump because it silently breaks downstream tooling pinned to 3.1.x for users who never use QUERY. Also flagged DRY gap: version string duplicated across handler.ts + 8 snapshot files with no single OPENAPI\_VERSION constant. Core fix (ALLOWED\_METHODS += "QUERY", @ts-expect-error removal) is correct and mergeable independent of the version bump.

### Gotcha

<!-- lore:019caf24-0b27-7fbb-8206-c853f62f4dd0 -->
* **Always branch from main when creating fix branches to avoid carrying unrelated commits**: When creating a new fix branch (e.g. \`fix/issue-196-...\`), always branch from \`main\` (or \`origin/main\`), not from a previous fix branch. If you branch from another fix branch that later gets merged/squashed into main, the new branch will carry the old commit with a different hash than what landed in main. This requires a rebase onto \`origin/main\` + force push to clean up. Fix if it happens: \`git rebase origin/main\` — git auto-skips previously applied (cherry-picked/squashed) commits. Then \`git push --force-with-lease\` to update the remote branch.

<!-- lore:019f07d4-ba55-7f42-bb22-c92f418c780d -->
* **GitHub Actions setup-node cache:pnpm fails without cache-dependency-path**: Trap: \`actions/setup-node@v4\` with \`cache: 'pnpm'\` looks complete but fails at post-job cleanup with 'Path Validation Error: Path(s) specified in the action for caching do(es) not exist'. Two distinct root causes: (1) Missing \`cache-dependency-path: pnpm-lock.yaml\` — always add this alongside \`cache: 'pnpm'\`. (2) \`pnpm install\` never runs in the job — if no pnpm store directory is created, there's nothing to cache and cleanup fails. Fix: either add \`pnpm install\` to the job, or remove \`cache: 'pnpm'\` and \`cache-dependency-path\` entirely from jobs that don't install dependencies (e.g. \`release.yml\`, \`publish.yml\`). In hono-usage-limiter, \`release.yml\` and \`publish.yml\` don't run \`pnpm install\` — cache config was removed from both.

<!-- lore:019fd5a6-d670-7d0e-bd54-b249378c0410 -->
* **hono-openapi getPathContext() regex substring bug — PR #225**: Trap: old \`getPathContext()\` (src/utils.ts) used \`path.match(key)\`, treating the context key as a regex and doing substring matching — looks correct because it works fine for exact paths, but silently matches unrelated routes sharing a trailing segment. Caused describeRoute context registered for \`/players\` to leak into \`/collections/players\`, and \`/module\` to leak into \`/module2/endpoint\` (issues #143, #119). Fix (PR #225): strip trailing wildcard from the context key (\`/players/\*\` → \`/players\`), then require \`path === prefix\` or \`path.startsWith(\\\`${prefix}/\\\`)\` for segment-boundary matching. Edge cases still needing test coverage: root-level key \`/\*\` (prefix becomes \`""\`, \`startsWith("/")\` always true — works but implicit), keys without a trailing wildcard from \`use('/')\` + \`basePath()\` (the exact issue #143 MRE), and path-param prefixes like \`/users/{id}\`.

<!-- lore:019fd5b6-e7ab-7eb0-9cf1-574c51574c58 -->
* **hono-openapi PR #226 hasValidation heuristic false-positives on manual params/body**: Trap: \`hasValidation = schema.requestBody || (schema.parameters?.length > 0)\` (used to gate auto-injecting a 400 validation-error response) looks correct because validator-derived specs do populate parameters/requestBody. Fix: manually-documented \`describeRoute({parameters:\[...]})\` or \`describeRoute({requestBody:{...}})\` with NO actual \`validator()\` call also populate those fields, so routes get a misleading auto-injected 400. Also affects global \`.use(validator(...))\` middleware leaking params onto sibling routes. Root cause: heuristic conflates 'has documented parameters/body in spec' with 'uses validator()' — these are indistinguishable once merged into the final OpenAPI schema object. Confirmed via probe tests before the real fix (see VALIDATION\_MARKER pattern) was applied in commit 58b9a80 on PR #226.

<!-- lore:019fd5b3-074e-7d5a-ac0e-8ac5913186eb -->
* **hono-usage-limiter deduct() result.success is always true — no atomic gating**: Trap: \`UsageDeductResult.success\` looks like it should reflect whether the account had sufficient balance, since \`deduct(amount, reason)\` returns a result object. Fix: both \`MemoryStore.deduct\` and \`D1Store.deduct\` do unconditional subtraction and hardcode \`success: true\` — it never checks or rejects insufficient balance, allowing negative \`usageRemaining\`. There is no atomic check-and-deduct (\`tryDeduct\`). Freestyle Cloud works around this by calling \`usage.check()\` as a soft gate before calling \`deduct()\` separately — this has a TOCTOU race under concurrency (two concurrent requests can both pass check() then both deduct). Recommended library fix: atomic \`tryDeduct()\` returning \`{success:false, remaining}\` on insufficient balance instead of unconditionally subtracting.

<!-- lore:019f07d4-ba59-7ead-85f7-e555bd3426be -->
* **hono-usage-limiter publish.yml needs actions:read permission for gh run list**: Trap: \`gh run list --workflow 'Package Artifacts' --branch 'release/x.y.z'\` in \`publish.yml\` returns HTTP 403 'Resource not accessible by integration' — looks like a branch or workflow name issue. Fix: the GITHUB\_TOKEN lacks \`actions: read\` permission. Add \`permissions: actions: read\` to the publish workflow job. In hono-usage-limiter run 28281468836, this caused 'Wait for package artifacts on release branch' to fail, blocking the publish step entirely.

<!-- lore:019fd5b3-0746-7b0a-adc3-d480152378b7 -->
* **hono-usage-limiter: changing defaultUsage doesn't update existing buckets**: Trap: changing \`UsageManagerConfig.defaultUsage\`/\`defaultWindowDurationMs\` looks like it should raise everyone's limit on next window rollover. Fix: \`resolveBucket()\`'s auto-refill only applies the code constant at first provisioning — window rollover refills \`usage\_remaining\` from the bucket's already-stored \`usage\_limit\` column, not the current config. Existing buckets never pick up a changed limit. In Freestyle Cloud, raising the free tier from 1,000→2,000 words/week required a hand-written migration (\`0009\_raise\_free\_word\_limit.sql\`: \`UPDATE usage\_buckets SET usage\_remaining = usage\_remaining + 1000, usage\_limit = 2000 WHERE usage\_limit = 1000\`) since hono-usage-limiter v0.1.0 has no \`reconcileLimit\`/\`setLimit\` API. Confirmed design gap.

<!-- lore:019f07ae-b9eb-7e7c-b3ed-2264ab17d0ee -->
* **hono-usage-limiter: totalConsumed JSDoc says 'lifetime' but resets on window/refill/provision**: Trap: \`UsageBucket.totalConsumed\` JSDoc (src/types.ts) says 'lifetime of this bucket', implying it accumulates forever. Fix: \`reset()\`, auto-refill (\`resolveBucket()\`), and \`provision()\` all set \`totalConsumed: 0\` — actual semantics are 'current window'. \`UsageBalanceInfo.totalConsumed\` JSDoc correctly says 'current window'. The \`UsageBucket\` JSDoc is misleading and should be corrected before publish.

<!-- lore:019f096b-0ba8-778f-83b9-f6ae5b89a0d4 -->
* **honojs/middleware monorepo: run tests per-package — root vitest fails due to typia-validator missing @typia/unplugin**: Trap: running \`vitest\` from monorepo root looks like it should run all package tests. Fix: \`packages/typia-validator/vitest.config.ts\` fails with \`ERR\_MODULE\_NOT\_FOUND\` for \`@typia/unplugin\`, causing \`AggregateError: Failed to initialize projects\` that aborts all tests. Root \`turbo.json\` has no \`test\` task. Always run tests from within the specific package directory (e.g., \`cd packages/standard-validator && node\_modules/.bin/vitest\`) or use \`--project\` filter. Pre-existing issue unrelated to any current work.

<!-- lore:019f033c-4bd4-7d47-83d4-bd15de15ebf4 -->
* **MemoryStore getLedger: sort by insertion index, not timestamp**: Trap: sorting ledger entries by \`created\_at\` timestamp (Date.now()) looks correct for newest-first ordering. Fix: entries created in rapid succession (e.g. in tests) get identical timestamps, making the sort unstable and tests flaky. \`MemoryStore.getLedger\` must sort by reversed insertion index instead of timestamp. Confirmed bug in \`src/memory.ts\` caught by test \`MemoryStore > should return ledger entries newest first\`. Note: class is now \`MemoryStore\` implementing \`UsageStore\` (renamed from \`CreditStore\`).

<!-- lore:019f07ae-b9e9-75e0-9929-c4d70f31d7bf -->
* **MemoryStore.getBucket returns mutable reference — silent state corruption**: Trap: \`MemoryStore.getBucket()\` (memory.ts) returns the actual object stored in the internal Map, which looks fine since it's typed as \`UsageBucket\`. Fix: consumer mutation (e.g. modifying returned bucket fields) silently corrupts store state because it's the same reference. Fix by returning \`{ ...bucket }\` spread. This is a pre-publish bug in hono-usage-limiter v0.1.0.

<!-- lore:019f0920-b562-781d-83e6-44f929072025 -->
* **Named function expression inside same-named outer function gets numeric suffix**: Trap: using a named function expression \`async function describeRoute(\_c, next) { ... }\` inside the outer \`describeRoute()\` function looks like it should set \`.name\` to \`'describeRoute'\`. Fix: JavaScript auto-suffixes the inner name to \`'describeRoute2'\` to avoid shadowing the outer function. Use \`Object.defineProperty(middleware, 'name', { value: 'describeRoute', configurable: true })\` instead — this bypasses the naming collision entirely. Applies to any helper function that creates a middleware with the same name as its enclosing function.

<!-- lore:019fd59b-13f1-7c47-ad3c-e84dbfe26bef -->
* **OpenAPI 3.2 changelog URLs 404 — use these instead**: Trap: fetching https://www.openapis.org/blog/2025/09/19/openapi-specification-3-2-released or https://learn.openapis.org/specification/versions.html for OpenAPI 3.2 release notes looks like the canonical URL pattern but both 404. Fix: find the actual blog post titled "Announcing OpenAPI v3.2" via the blog index (URL isn't predictable from date), and use https://learn.openapis.org/upgrading/v3.1-to-v3.2.html for the official migration guide — confirms 3.2 is fully backward-compatible with 3.1, no breaking changes. Recurred 11+ times across sessions investigating hono-openapi's OpenAPI 3.2 support.

<!-- lore:019f079e-b492-75a2-98bb-9cf427bde55c -->
* **pkg-pr-new requires GitHub App installation — 404 on /check is not a code error**: Trap: \`pnpm exec pkg-pr-new publish "."\` failing with \`{"statusCode":404,"message":"There is no workflow defined for ..."}\` looks like a misconfigured workflow or wrong publish path. Fix: the pkg-pr-new GitHub App must be explicitly installed on the repo at https://github.com/apps/pkg-pr-new — it's not inherited from org-level installs. No code change needed. In hono-usage-limiter, the 'Release preview' CI workflow fails for this reason while 'CI Tests' passes fine.

<!-- lore:019fd569-38fd-7bb4-b09c-e1394c18eac5 -->
* **pnpm/action-setup@v4 requires packageManager field in package.json**: Trap: \`pnpm/action-setup@v4\` without an explicit \`version:\` input looks like it should just work — and it does, but only by reading the \`packageManager\` field in package.json. If that field is missing (as in hono-rate-limiter before craft setup), the action fails outright. Fix: add \`"packageManager": "pnpm@X.Y.Z"\` to package.json before adopting any workflow using this action (e.g. craft's package-artifacts.yml). Confirmed when adding craft release automation to rhinobase/hono-rate-limiter — hono-openapi already had the field and needed no change.

<!-- lore:019f096b-0ba5-747b-b8ba-7ea0fb3ee823 -->
* **sValidator hook typed as InferOutput but runtime passes InferInput — issue #1990**: Trap: \`hook\` parameter in \`sValidator\` typed as \`Hook\<StandardSchemaV1.InferOutput\<Schema>>\` looks correct since hooks receive validated data. Fix: runtime passes \`value\` (raw input, pre-parse) to hook in both success and failure branches — should be \`Hook\<StandardSchemaV1.InferInput\<Schema>>\`. Fixed in honojs/middleware PR #2008 alongside the \`InferInput\` literal-union fix. Affects \`@hono/standard-validator@0.2.2\` and earlier. Issue filed 2026-06-22 by @tomkel as honojs/middleware#1990.

<!-- lore:019f0932-9248-7b99-8d54-923154aeaec8 -->
* **validator() in-type maps to wire types for non-json targets — RPC loses schema-derived literals**: Trap: \`validator('query', z.object({ sort: z.enum(\['name','type']) }))\` looks like it should give the RPC client \`sort: 'name' | 'type'\`. Fix: the old \`in\` side mapped through \`ValidationTargets\['query']\[K2]\` = \`string | string\[]\`, discarding the enum. The fix (honojs/middleware PR #2008, \`@hono/standard-validator\`) ports the \`InferInput\` utility from \`@hono/zod-validator/utils.ts\` — \`IsLiteralUnion\` preserves literal unions and optional unions as-is, only falling back to wire types for wide types. Uses \`\[In] extends \[ValidationTargets\[K]] ? In : InferInput\<In, K>\` (tuple brackets prevent distributive conditional). hono-openapi's \`validator()\` will inherit this fix automatically once upstream lands. For \`json\` target, using \`Out\` directly is always safe — JSON has no string-only wire constraint.

<!-- lore:019fd5b6-e7c0-7d7a-b022-fb90c931148e -->
* **vitest probe test files must live under src/\_\_tests\_\_, not /tmp**: Trap: writing a throwaway probe/spike test file in /tmp (e.g. /tmp/probe.test.ts) to empirically validate a hypothesis before writing real tests looks convenient since it's outside the tracked repo. Fix: vitest's project root/include-exclude config doesn't scan /tmp, so it fails with 'No test files found'. Place probe files inside the package's actual test directory (e.g. src/\_\_tests\_\_/) so vitest picks them up, then delete the probe file after validation. Confirmed in hono-openapi.

### Pattern

<!-- lore:019fd569-38d9-7807-8e91-35eac138b80b -->
* **Craft release automation setup for rhinobase repos**: Steps: 1. Copy 5 files verbatim from a repo with working craft setup (e.g. hono-credit/hono-usage-limiter): .craft.yml, .github/workflows/{changelog-preview,package-artifacts,publish,release}.yml — reuses a battle-tested config instead of authoring from scratch. 2. Verify target package.json has "packageManager": "pnpm@10.0.0" (or matching) — pnpm/action-setup@v4 in package-artifacts.yml needs this when no explicit version input is given; missing it fails pnpm setup. 3. Confirm target repo is on clean main with no existing .craft.yml/CHANGELOG.md. 4. Branch as ci/add-craft, commit "ci: add craft release system", push, open PR ready for review (not draft). Gotchas: - hono-rate-limiter lacked packageManager — add it in the same PR since the new workflow requires it. - Workflows are inert after merge until: Craft GitHub App installed/authorized with CRAFT\_APP\_ID var + CRAFT\_APP\_PRIVATE\_KEY secret, and npm trusted publishing (OIDC) configured. These are manual GitHub/npm UI steps, not doable via PR. Verify: - \[ ] Diff copied files byte-identical to source repo. - \[ ] package.json valid JSON after edit. - \[ ] PR opened, workflows visible in Actions tab.

<!-- lore:019fd569-d1be-752a-9bf1-b9321951e8f5 -->
* **Craft release automation setup: replicate from hono-credit reference repo**: Steps: 1. Copy 5 files verbatim from hono-usage-limiter (aka hono-credit, the reference craft implementation): \`.craft.yml\`, \`.github/workflows/{changelog-preview.yml, package-artifacts.yml, publish.yml, release.yml}\` — this is the canonical rhinobase craft config. 2. Verify target repo's package.json has \`"packageManager": "pnpm@X.Y.Z"\` — \`pnpm/action-setup@v4\` in package-artifacts.yml requires it \[\[019fd569-38fd-7bb4-b09c-e1394c18eac5]]. Add if missing. 3. Branch from main as \`ci/add-craft\`, commit \`ci: add craft release system\`, push, open PR via \`gh pr create\` ready for review (not draft). 4. Open one PR per target repo — don't bundle multi-repo changes into a single PR. Gotchas: - Existing test.yml secrets (e.g. UPSTASH\_REDIS\_REST\_URL) don't block craft since package-artifacts.yml only builds+packs, never runs tests. External follow-ups (manual, not via PR): install/authorize the Craft GitHub App + set \`CRAFT\_APP\_ID\` var + \`CRAFT\_APP\_PRIVATE\_KEY\` secret per repo; configure npm trusted publishing (OIDC) per package. Verify: - \[ ] All 5 files byte-identical diff vs hono-usage-limiter source. - \[ ] package.json valid JSON with packageManager field. - \[ ] PR opened non-draft.

<!-- lore:019f096b-0bab-7db3-b179-49530bbe1760 -->
* **hono-openapi PR #234 response: upstream fix in honojs/middleware supersedes in-repo fix**: When a contributor opens a PR on rhinobase/hono-openapi to fix \`validator()\` \`in\` type inference (e.g., PR #234 by brilliantnz), the correct response is: (1) request changes citing coercion regression risk for string-based targets, (2) point to the upstream fix in \`@hono/standard-validator\` (honojs/middleware PR #2008), (3) explain hono-openapi will inherit the fix automatically once upstream lands, (4) close the PR. The \`json\` target is always safe to use \`Out\` directly — JSON has no string-only wire constraint. String-based targets (query, param, header, cookie) require \`InferInput\` to avoid breaking coercion schemas.

<!-- lore:019caf8e-1b12-7ea5-a7ec-d4b7570caf58 -->
* **hono-openapi release process: version bump, commit, tag, push**: To release a new version of hono-openapi: 1. Update \`version\` in \`package.json\` 2. Commit with message \`released v{X.Y.Z}\` (matching the most recent convention — earlier releases used \`release v{X.Y.Z}\` but recent ones use \`released\`) 3. Create git tag \`v{X.Y.Z}\` 4. Push both commit and tag: \`git push origin main --tags\` Tag naming convention: \`v{X.Y.Z}\` (e.g. \`v1.3.0\`). Older pre-1.0 tags used \`core@{X.Y.Z}\` format but that's no longer used.

<!-- lore:019caf20-58f2-7228-b85d-b9dcc9e18aef -->
* **hono-openapi vendor-specific auto-injection pattern in resolver()**: hono-openapi's \`resolver()\` function uses a vendor detection + injection pattern to auto-fix known vendor limitations. The pattern: 1. Detect vendor via \`schema\["~standard"].vendor\` (e.g. \`"zod"\`, \`"arktype"\`) 2. For the \`toOpenAPISchema\` path, inject vendor-specific defaults into the nested \`options\` object 3. Preserve user-provided options — if the user already supplied the relevant option (e.g. \`override\`, \`fallback\`), don't replace it 4. Further refine detection with schema-level checks (e.g. \`"\_zod" in schema\` for Zod v4 vs v3) Current auto-injections: - \*\*ArkType\*\* (\`injectArktypeFallback\`): Injects \`{ options: { fallback: (ctx) => ctx.base } }\` to handle morph schemas (PR #222) - \*\*Zod v4\*\* (\`injectZodV4DateOverride\`): Injects \`{ options: { unrepresentable: "any", override: zodV4DateOverride } }\` to handle \`z.date()\` (PR #224) Both follow the same structure: a private helper function that checks user options at both flat and nested levels, returns \`{ options: { ... } }\` or \`undefined\`, and gets spread into the \`toOpenAPISchema\` context.

<!-- lore:019f0920-b567-7915-8233-54b4bbd667af -->
* **hono-openapi: naming middleware functions for observability via Object.defineProperty**: Both \`describeRoute()\` and \`validator()\` in \`src/middlewares.ts\` use \`Object.defineProperty(middleware, 'name', { value: '\<name>', configurable: true })\` to set meaningful function names. Named function expressions are rejected for this use case because a same-named inner function inside the outer helper gets a numeric suffix (e.g. \`describeRoute2\`). The \`configurable: true\` flag is required so downstream code (e.g. Sentry's \`@sentry/hono\`) can read and display the name in spans. \`describeResponse()\` does NOT get this treatment — it wraps a user-provided handler, so overwriting its name would harm tracing. \`loadVendor\` and \`resolver\` are utility functions that don't appear in traces. Only \`describeRoute\` and \`validator\` require this pattern.

<!-- lore:019fd5b3-0754-758b-bc62-fae922dcc01e -->
* **hono-usage-limiter known design gaps — P0-P3 priority list**: From reviewing real usage in Freestyle Cloud, ranked gaps for future hono-usage-limiter work: P0: atomic tryDeduct() (deduct().success is always true, no real gating — see deduct() gotcha entry); atomic window refill — \`resolveBucket()\` in manager.ts does read-then-write on expiry, races under concurrent requests in the same colo, can double-refill or clobber totalConsumed. P1: no built-in caching — every serious consumer hand-builds a CachedUsageStore (2-min Cache API layer, D1 always authoritative for deduct/getLedger); \`UsageStoreFactory\` is untyped \`(c: unknown) => UsageStore\`, forcing \`as Context\<Env>\` casts at every call site — should be generic over Hono Env. P2: \`defaultUsage\`/\`defaultWindowDurationMs\` ignored after first provision, requiring hand-written SQL migrations to change limits; no bulk/admin operations — admin analytics bypass the lib entirely via raw SQL against usage\_buckets/usage\_ledger. P3: getLedger has no limit clamp; UnstorageStore.updateBucket is O(n) scan (needs id→ownerId reverse index like the ledger); reason field is an untyped free string; no credit()/refund path (deduct requires positive amount only).

<!-- lore:019fd5b6-e7ba-7b6c-95fe-802c8d062f1f -->
* **VALIDATION\_MARKER: enumerable string key survives mergeSpecs, symbols don't**: hono-openapi's \`mergeSpecs\` (src/utils.ts) rebuilds spec objects via \`Object.entries(spec)\`-based reduce, which silently drops symbol keys and non-enumerable properties. To reliably tag validator-derived specs (so downstream code like \`removeExcludedPaths\` can distinguish real \`validator()\` usage from manually-documented \`describeRoute\` params/body), use an enumerable STRING key marker (e.g. \`VALIDATION\_MARKER = "\_\_HonoOpenAPIValidator\_\_"\` set as \`docs\[VALIDATION\_MARKER] = true\` in \`getSpec\`'s validator branch), not a Symbol or non-enumerable property — those get silently stripped by the merge and the marker never reaches \`removeExcludedPaths\`. Boolean values propagate correctly through repeated \`mergeSpecs\` calls (else-branch direct assignment). Always \`delete schema\[VALIDATION\_MARKER]\` before emitting the final spec to avoid leaking it into output.

### Preference

<!-- lore:019f07c9-4af0-706a-afe1-b16e13218213 -->
* **Always confirm architectural constraints before proposing solutions**: When reviewing or designing code, the user explicitly confirms fundamental architectural constraints (e.g., 'store is always a pre-constructed instance', 'D1 bindings are always request-time in Cloudflare Workers', 'store is never re-evaluated') before accepting or rejecting proposed changes. The user expects the assistant to surface these constraints as questions or findings, then waits for explicit confirmation before proceeding. Do not assume or infer runtime behavior — always surface architectural assumptions and get the user to confirm them as facts before building on them.

<!-- lore:019f0937-24fb-7a42-87ba-a83a37f6d181 -->
* **Always demand complete, exact type definitions — never summaries or paraphrases**: When discussing TypeScript types, the user expects full, verbatim type definitions — not paraphrases, abbreviations, or prose descriptions. This applies to utility types (e.g., \`IsLiteralUnion\`, \`HasUndefined\`, \`InferInputInner\`), generic signatures, and mapped type constructions. The user explicitly demands exact code when working on type-level proposals or implementations. When reading source files or node\_modules declarations, always quote the complete definition as it appears in the source. Never substitute a summary like 'checks if T is a literal union' for the actual TypeScript expression.

<!-- lore:019f0929-6b16-7ea0-add5-02954f85d44a -->
* **Always explore and map codebase architecture before writing any code**: Before implementing any fix, feature, or change, the user consistently reads all relevant source files, maps directory structure, traces function call chains, and understands existing patterns. This applies even when the task seems straightforward. The exploration phase includes: listing directory contents, reading full source files (not just relevant sections), identifying imports/exports, tracing data flow, and checking existing tests. Only after this full architectural understanding does the user proceed to implementation. Never skip straight to writing code — always explore first.

<!-- lore:019f0350-70e5-7562-ac49-08a44d04ce9b -->
* **Always explore and read existing files thoroughly before making changes**: Before implementing or modifying anything, the user consistently requests (or the assistant performs) a thorough read of all relevant existing files — directory trees, config files, workflows, source files, and documentation. This applies whether starting a new feature, renaming a package, or understanding a release system. The user expects the assistant to fully understand the current state of the codebase (including stale/incorrect content) before proposing or applying any changes. Skipping this exploration phase is not acceptable — even when the task seems straightforward.

<!-- lore:019f0932-923f-7f86-8d58-e7196ca39bec -->
* **Always explore codebase structure before implementing or modifying code**: Before writing any implementation, the user consistently performs thorough codebase exploration: listing directory contents, reading source files, checking package.json for dependencies/scripts/exports, examining test files, and understanding build tooling. This applies to both familiar repos (returning to hono-openapi multiple times) and new ones. The user reads actual file contents rather than relying on summaries, and specifically investigates type signatures, exports, peer dependencies, and existing patterns before asking for changes. When exploring, the user wants comprehensive coverage: project structure, tooling, types, tests, and relevant node\_modules type definitions.

<!-- lore:019f091d-f58f-7cfa-81a0-4b45dde076ec -->
* **Always explore codebase structure thoroughly before starting any task**: At the start of every session, the user requests or expects a comprehensive codebase exploration before any implementation or review work begins. This includes: listing directory structure, reading all source files in full, identifying exports/entrypoints, checking test files, reviewing build/tooling config, and understanding existing types and interfaces. The exploration is systematic — covering src/, tests, package.json, config files, and dist/ if present. Only after this full picture is established does the user proceed to the actual task. When assisting, always perform thorough upfront exploration without being asked, covering all relevant files before proposing or making changes.

<!-- lore:019f07c6-fe0a-75a2-b9c3-3a09ef8ae82e -->
* **Always identify and flag request-time vs. registration-time initialization conflicts**: When reviewing middleware or library architecture, always identify resources only available at request time (e.g. \`c.env.DB\` in Cloudflare Workers) being required at middleware registration time. Prefer or flag support for a factory/lazy pattern: \`store: (c) => new D1Store({ db: c.env.DB })\`. In hono-usage-limiter, \`UsageManagerConfig.store\` now accepts \`UsageStore | UsageStoreFactory\` — middleware resolves via \`typeof storeOrFactory === 'function' ? storeOrFactory(c) : storeOrFactory\` per-request. \`UsageManager\` constructor always receives a concrete \`UsageStore\` (via \`UsageManagerOptions\`). Confirmed: D1 bindings are always request-time in Cloudflare Workers; \`store\` in closure is never re-evaluated after \`usageManager(config)\` call.

<!-- lore:019f079e-b4ce-7a42-8c41-acd8590d90e4 -->
* **Always investigate CI failures by diagnosing root cause before proposing fixes**: When CI is failing, the user expects a thorough diagnostic process: identify which specific workflow/step failed, extract the exact error message, check environment details (runner, Node version, tool versions), and pinpoint the root cause (e.g., missing GitHub App installation, misconfiguration) before proposing a fix. The user wants the fix clearly distinguished as a code change vs. an external action (e.g., installing a GitHub App). Fixes should be minimal and targeted — only address the confirmed root cause, not unrelated issues observed during investigation.

<!-- lore:019f07da-38ab-7d6f-afb1-4ef024d97f55 -->
* **Always investigate CI/CD workflow failures by reading full workflow file contents and tracing root causes before applying fixes**: When CI workflows fail, the user systematically: (1) identifies the failing run/job by ID, (2) reads the full content of relevant workflow YAML files, (3) compares across multiple workflow files to find inconsistencies (e.g., pnpm install present in some but not others), and (4) traces the exact root cause before applying a minimal targeted fix. The user expects the assistant to read actual file contents rather than guess, cross-reference related workflows for patterns, and explain why the failure occurred (not just what failed). Fixes should be surgical — removing or adding only the specific misconfigured option rather than rewriting the workflow.

<!-- lore:019f093b-bb6e-7eb1-b2f4-f2f2199ab9e6 -->
* **Always investigate GitHub issues in the rhinobase/hono-openapi ecosystem by reading workflow.md and core source files first**: When working on the rhinobase/hono-openapi repository, the user consistently reads workflow.md first before taking any action on issues or PRs. This file contains the project's contributing guidelines including branch naming conventions, test-first approach, coding standards, PR conventions, and issue comment tone. Always fetch and read workflow.md at the start of any session involving this repo before reading source files, writing code, or posting comments.

<!-- lore:019f0934-e36f-7664-b0d6-8e136757666e -->
* **Always investigate type system internals by reading source .d.ts files directly**: When working on TypeScript type issues in hono-openapi, the user consistently reads raw \`.d.ts\` files from \`node\_modules\` to understand generic signatures, type flows, and constraints — rather than relying on documentation or summaries. This applies especially when debugging type parameter mismatches (e.g., \`In\`/\`Out\`, \`ValidationTargets\`, \`Input\`, \`JSONParsed\`). The user expects the agent to proactively locate and read the relevant declaration files (e.g., \`@hono/standard-validator/dist/index.d.ts\`, \`hono/dist/types/types.d.ts\`) and report exact generic signatures, not paraphrased descriptions.

<!-- lore:019f091f-5642-731b-99fc-e4b60cee88c0 -->
* **Always read workflow.md before starting any issue or PR work**: When working on GitHub issues or PRs for a project, the user expects the assistant to read the project's workflow.md (or contributing guide) before writing any code or taking action. This file governs branch naming conventions, test-first approach, PR conventions, issue comment tone, and when to stop (e.g., if a bug isn't reproducible). The assistant should treat workflow.md as mandatory context that shapes all subsequent decisions, not an optional reference.

<!-- lore:019f07ae-ba25-7490-a25b-b2e0d0c5bb24 -->
* **Always request a full code review before publishing or finalizing a package**: Before finalizing a commit, push, or release, the user consistently requests a full review of all modified/created files. This review covers: final file states with line counts, all exports and their types, key implementation details (method signatures, guards, defaults), test coverage (describe blocks, test counts, what each test verifies), and any remaining issues. The assistant should display every changed file's confirmed final state in detail, not just a summary. This happens as a checkpoint step after a batch of changes and before any git or publish operations.

<!-- lore:019f07bf-34d4-7c4d-a2cc-22142e5c47f2 -->
* **Always request a full review of all changed files before finalizing work**: After completing a significant batch of changes (renames, refactors, new features, or fixes), the user consistently requests a full review of every modified or created file to verify the final state. This happens before committing/pushing or declaring work done. The review should display the actual final content or a detailed summary of each file — including line counts, key types/symbols, method signatures, and any notable implementation details — so the user can confirm correctness before proceeding.

<!-- lore:019f0941-360b-7159-987f-679bc32b1e57 -->
* **Always research ecosystem dependencies exhaustively before proposing architectural solutions**: When investigating a technical problem (e.g., type inference in hono-openapi), the user consistently demands deep, multi-layered research before any solution is proposed: exact package versions, full type declaration file contents, GitHub issues across all related repos, upstream spec changes (e.g., StandardJSONSchemaV1 in @standard-schema/spec v1.1.0), and how each dependency interacts. The user expects the assistant to surface newly tagged/unreleased versions, read raw source files (not summaries), and reassess architectural conclusions when new ecosystem facts emerge. Only after this exhaustive mapping does the user consider solution design.

<!-- lore:019f093a-59c5-72da-b7de-c4cb81d570dd -->
* **Always retrieve and record exact full type definitions from source files**: When investigating a codebase or library, the user consistently wants the assistant to retrieve and record the complete, exact content of type declaration files (\`.d.ts\`) rather than summaries or paraphrases. This applies especially to utility types, generic signatures, and interface definitions. The user expects full type definitions to be captured verbatim so they can be used directly in proposals, implementations, or analysis. The assistant should proactively fetch relevant type files (e.g., from \`node\_modules\`) and present their full content, not abbreviated versions.

<!-- lore:019f0932-9276-7a07-b158-90c34a477e80 -->
* **Always review GitHub PRs by deeply analyzing type-level correctness and edge cases**: When reviewing PRs on rhinobase/hono-openapi, the user reads the full diff, traces generic type parameters through the entire call chain, checks claims against actual source (ValidationTargets, StandardSchemaV1, HasUndefined, etc.), and actively challenges or corrects the PR author's assertions. The user expects the reviewer to verify each claim independently rather than accept them at face value, and to identify cases where the proposed change may break or not fully address the stated problem (e.g., checking whether \`In\` is always \`string | string\[]\` for non-json targets, or whether \`HasUndefined\<In>\` is always true). Focus on type inference correctness, not runtime behavior.

<!-- lore:019f096a-9950-7a40-8477-9ffeb7c24411 -->
* **Always review open PRs for correctness, completeness, and test coverage before merging**: When asked to review PRs, the user wants a thorough technical verdict covering: correctness of the fix, edge cases the author acknowledged or missed, quality of tests (or lack thereof), and whether the PR is safe to merge as-is. The user expects the assistant to read the actual diff, trace the code path, and give a clear merge/don't-merge recommendation with reasoning. PRs that are 'vibe-coded' or lack formal test coverage should be flagged. The user works in the rhinobase/hono-openapi repo and applies this review standard consistently across contributor PRs.

<!-- lore:019f0355-7b11-70d1-892a-aac7eb943ec5 -->
* **Always run tests and build verification before committing and pushing**: After implementing a fix or feature, the user consistently runs the full test suite and build process before committing and opening a PR. Pattern: implement → run tests (fix failures) → run build → commit → push → create PR. Tests for middleware naming are explicitly not needed — the user removed the \`describe('middleware naming')\` block, preferring to keep tests focused on functional behavior rather than internal implementation details like function \`.name\` properties.

<!-- lore:019f0962-569d-78a9-9978-0db5bd0d5b1f -->
* **Always run tests targeting a specific package to avoid unrelated monorepo failures**: When working in a monorepo, the user expects tests to be scoped to the specific package being worked on rather than run from the root. This avoids failures caused by unrelated packages (e.g., missing dependencies in other packages like typia-validator). Use the package's local test runner directly (e.g., \`node\_modules/.bin/vitest\` from the package directory, or pass a project filter) instead of running vitest/turbo from the monorepo root. This pattern applies whenever a root-level test run would aggregate all projects and fail due to unrelated setup issues.

<!-- lore:019f07c5-28ae-7fd3-8c44-c8e68c411dc2 -->
* **Always start new sessions with a thorough codebase exploration before any implementation work**: When beginning work on a project (even one worked on in prior sessions), the user consistently requests a comprehensive codebase exploration first — covering directory structure, source files, types/interfaces, middleware patterns, store implementations, and how components connect. This happens at the start of each session regardless of prior context. When exploring, the user wants specifics: not just file names but actual content of key files (types, middleware signatures, store interfaces, registration patterns). Provide a thorough read-through of all relevant source files before proposing any changes or implementations.

<!-- lore:019f0357-c40b-7c95-b3f4-b4e7735af6c6 -->
* **Always verify with tests and build after completing rename/refactor tasks**: After completing any renaming or refactoring work (type renames, package renames, variable renames), the user expects a test run and build verification before marking the work done. This applies after each major refactor milestone — not just at the end. The user also expects the assistant to run tests proactively without being asked, confirm the exact test count (e.g., '21 tests passing, 0 failures'), confirm the build tool output (e.g., 'pkgroll --clean-dist succeeded'), and report the package name as shown in the test runner output.

<!-- lore:019f095f-7465-7e5c-b176-86c2de688f38 -->
* **Always work within existing fork directory paths without re-cloning or re-confirming setup**: When the user provides a local fork path (e.g., \`/Users/am/dev/mathuraditya724/hono-middleware\`), they expect the agent to work directly in that directory without asking for confirmation, re-cloning, or verifying prerequisites beyond a quick branch/status check. The user also expects the agent to read all relevant source files thoroughly before making changes, cross-reference related packages (e.g., zod-validator utilities when fixing standard-validator), and run tests using the package-local binary (e.g., \`node\_modules/.bin/vitest\`) when workspace-level scripts are unavailable.

<!-- lore:019f0955-5be8-7e79-a6b3-10cd4723611c -->
* **Always work within locally cloned repositories at specific filesystem paths**: The user consistently works with locally cloned repositories at explicit paths (e.g., \`/Users/am/dev/rhinobase/hono-openapi\`, \`/Users/am/dev/mathuraditya724/hono-middleware\`). They expect the agent to explore the actual filesystem — reading source files, node\_modules, package.json, dist/ type declarations — rather than relying on npm registry or GitHub. Always confirm the local path, read files directly from disk, and treat installed package versions (from node\_modules) as the source of truth. When investigating type issues, read the full file contents rather than summaries.

<!-- lore:019fd5be-f213-7298-b873-0f2d0fa6d72c -->
* **Follow the established git workflow (branch, PR, review)**: Behavioral pattern detected across 3 sessions (action: enforced-workflow). The user consistently demonstrates this behavior.

<!-- lore:019fd5b6-e7d6-7623-be7e-be86c80e487b -->
* **Namespace internal marker/property keys, avoid generic names**: When adding an internal marker property/key to a shared object (e.g. a flag distinguishing validator-derived specs), Aditya Mathur rejects generic names like \`\_\_hasValidation\_\_\` and prefers a project/package-namespaced name, e.g. \`\_\_HonoOpenAPIValidator\_\_\`. Apply this convention to any new internal marker keys added to shared/merged objects in hono-openapi or similar libraries.

<!-- lore:019fd5b6-e7d0-792e-9df4-d16526e75793 -->
* **Only merge a PR when explicitly instructed in that message; otherwise pause for sign-off**: Aditya Mathur distinguishes 'review/fix/prepare for merge' from 'merge it'. When the user says 'make this merge ready' or 'do a final review', implement fixes/tests, verify fully (tests/build/lint/CI), but do NOT merge — push and report status, then ask for sign-off. Only merge (via \`gh pr merge\`) when the user explicitly says 'merge it' in that same instruction. Observed: PR #225 was merged only after explicit 'if everything looks good merge it'; PR #226's bug fix was pushed but merge was explicitly withheld pending user review of the fix approach.
